import Navigation from "../Navigation/Navigation";
import "./Header.css"
const Header = () => {
    return (
        <header>
            <Navigation />
            <h2>This is header</h2>
        </header>
    );
}

export default Header;